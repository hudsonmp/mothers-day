# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp

# Local server
.cache/

# Personal photos go here — keep .gitkeep, ignore actual files so private photos
# of mom don't end up on a public GitHub repo by accident.
assets/photos/*
!assets/photos/.gitkeep
# Vercel CLI link state + per-machine deploy excludes
.vercel
.vercelignore

# Personal scenes backup (don't commit)
.hudson-personal-scenes.json
hudson-scenes.json
